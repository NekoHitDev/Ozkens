logging:
  level:
    root: info
    com.nekohit.neo.snapshot: debug
spring:
  jpa:
    show-sql: false
    hibernate.ddl-auto: create-drop
    properties:
      hibernate:
        dialect: org.hibernate.dialect.H2Dialect
        format_sql: true
  datasource:
    driver-class-name: org.h2.Driver
    url: jdbc:h2:mem:archive_dag_test
    username: sa
    password: password
neo:
  n3:
    rpc-base: # TODO Finish this
